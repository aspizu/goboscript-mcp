# Userscript package

This package provides a browser ES6 bundle for connecting to the Goboscript socket server using socket.io-client.

## Build

    bun install
    bun run build

The output bundle will be in `dist/userscript.js`.
